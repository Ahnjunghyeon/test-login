import "./App.css";
import Navbar from "./components/CustomNavbar"; // Navbar 컴포넌트를 import합니다.

function App() {
  return (
    <div className="App">
      <Navbar /> {/* Navbar 컴포넌트를 추가합니다. */}
    </div>
  );
}

export default App;
